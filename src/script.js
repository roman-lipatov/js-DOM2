import { initGallery } from './gallery.js';

const gallery = document.getElementById('gallery');
const imagesPerRow = 4; // Кількість зображень в рядку

// Ініціалізувати галерею
initGallery(gallery, imagesPerRow);